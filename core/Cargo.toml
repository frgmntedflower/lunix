[package]
name = "lunix-core"
version = "0.1.0"
edition = "2021"
description = "the home — module discovery, menu, launcher"

[[bin]]
name = "lunix-core"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
anyhow = "1"
