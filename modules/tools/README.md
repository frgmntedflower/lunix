# tools

everything else. RE tools, editors, system utilities, security
tooling that isn't specifically networking.

empty for now. modules get added here as submodules once they
exist, each one its own repo, own README. default language is c,
go if it's a security tool where shipping fast matters more than
staying minimal.

    git submodule add <repo-url> modules/tools/<name>
