# networking

network tools and modules. scanners, protocol tools, anything
that talks over a wire.

empty for now. modules get added here as submodules once they
exist, each one its own repo, own README. default language is go,
static binaries, easy to ship. c where more control is needed.

    git submodule add <repo-url> modules/networking/<name>
