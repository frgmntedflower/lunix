# graphics

rendering, display, anything visual. retro renderers, framebuffer
tools, whatever touches pixels.

empty for now. modules get added here as submodules once they
exist, each one its own repo, own README. default language is c,
asm for inner loops that need it.

    git submodule add <repo-url> modules/graphics/<name>
